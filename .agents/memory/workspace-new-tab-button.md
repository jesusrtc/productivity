# Workspace new-tab button

Keep a small, plain + button immediately after the open top-level tabs. Its
picker selects a registered vault, creates a workspace in that vault, or opens
an existing workspace. Vault navigation still opens a section beneath Home.
The create form captures the selected vault when opened so navigation cannot
redirect the creation request to another vault.
