# Vault overview prioritizes recent workspaces

The user wants every vault tab to show Workspaces first, across the full width,
with appearance, configuration, and agents below. Sort workspaces by most recent
navigation, newest first. Browser-local visit timestamps use the absolute
workspace path so identical names in separate vaults stay independent; unseen
workspaces fall back to display-name order.
