# Sidebar file history includes working tree and notebook review

Recent-file rows expose a visible GitHub-mark action that opens the shared Git
history modal. History must put uncommitted staged, unstaged, or untracked
changes before commits. For `.ipynb`, return semantic cell revisions and render
changed source and outputs in Before/After columns instead of raw notebook JSON.
