# File modal sort names and extension sorting

Use Modified time desc/asc and Created time desc/asc, not newest/oldest or
first/last. Name uses A–Z/Z–A. Include Type (extension), alphabetical by
case-insensitive extension, with filename ties and extensionless files first.
Modified time desc remains the default, with saved choices per file and root.
