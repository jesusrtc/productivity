# Home overview is a directory

The user wants Home Overview to show only SSD: the vault name, its path below,
and an indented list of workspace paths. Use plain selectable text, no links
and no workspace display names. Do not bring back task counts, task creation,
attention cards, changed areas, commits, or activity summaries. The later
SSD-only request supersedes showing Lab/Assistant paths and all vaults.
