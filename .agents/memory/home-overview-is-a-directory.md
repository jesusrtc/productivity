# Home overview is a directory

Home Overview shows the current client’s Lab framework path and Assistant
folder path, followed by every registered vault and its workspace paths.
Keep the Lab and Assistant entries even though they are not ordinary registered
vaults. All paths come from that client’s configuration and vault registry.
SSD was an example of the layout, never a filter. Show each vault name, its path
below, and an indented list of workspace paths. Use plain selectable text, no
links and no workspace display names. Do not bring back task counts, task
creation, attention cards, changed areas, commits, or activity summaries.
