# Files section exposes file creation

Every project, repository, workspace, and framework Files section should expose
a visible `＋ File` action in its header. It opens the shared new-file dialog at
the currently selected file root; secondary-click creation remains available
for creating inside a specific folder.
