# Terminal tmux names hide vault identity

Current Lab-owned tmux names are human-facing
`neurona-<workspace>-<tab>-<hash6>` names. Do not restore a visible vault
segment: vault ownership remains in the deterministic hash and runtime
registry. Continue discovering the previous `neurona-<vault>-...` and
older `lab-...` forms so live sessions survive framework upgrades.
