# Terminal task summary gets its own multiline block

Show the active agent's latest task in a dedicated block between the compact
connection-status row and the terminal. Reserve three lines and clamp at four;
do not squeeze the TL;DR into the one-line connection strip.

