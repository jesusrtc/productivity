# Local Lab client settings. Copy to .env and edit for this checkout.
# .env is intentionally ignored so each installation can use its own port.
LAB_PORT=3333
# One client-owned task database shared by every registered workspace.
LAB_ASSISTANT_HOME=/absolute/path/to/assistant
